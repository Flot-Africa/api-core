package africa.flot.domain.model.enums;

public enum ReminderLevel {
    FIRST,                // Première relance (douce)
    SECOND,               // Deuxième relance (plus ferme)
    THIRD,                // Troisième relance (sérieuse)
    FINAL                 // Relance finale (ultimatum)
}